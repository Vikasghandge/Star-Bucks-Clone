apiVersion: apps/v1
kind: Deployment
metadata:
  name: starbucks-deployment
spec:
  replicas: 1  # Number of desired replicas
  selector:
    matchLabels:
      app: startbucks
  template:
    metadata:
      labels:
        app: startbucks
    spec:
      containers:
      - name: startbucks
        image: ghandgevikas/hoststar:latest
        ports:
        - containerPort: 3000  # Port your application listens on